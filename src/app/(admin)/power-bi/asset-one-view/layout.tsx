export const metadata = {
  title: "Asset One View Power BI | CDP Dashboard",
};

export default function PowerBiAssetOneViewLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}