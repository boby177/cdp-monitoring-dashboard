export const metadata = {
  title: "Fit to Work Power BI | CDP Dashboard",
};

export default function PowerBiFitToWorkLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}