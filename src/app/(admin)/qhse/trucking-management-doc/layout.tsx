export const metadata = {
  title: "Trucking Management Document | CDP Dashboard",
};

export default function PowerBiKirLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}